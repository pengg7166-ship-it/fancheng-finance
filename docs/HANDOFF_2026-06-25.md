# 会话交接 · 2026-06-25

> **用途：** 复制粘贴到新 Cursor 聊天窗口，快速恢复上下文。  
> **工作区：** `E:\FanchengFinance\source\fancheng-finance`  
> **数据根目录：** `E:\FanchengFinance\data`（`FANCHENG_DATA_DRIVE=E`）  
> **成交导出：** `E:\交易记录`  
> **生产启动：** `E:\FanchengFinance\启动梵澄金融-安全.bat`  
> **生产权重版本：** **v1.34.8**（`outlook-logistic-weights.json`，本 quant 会话未改权重）  
> **Git：** 本 checkpoint **未 commit**；P0 修复在 commit **`243a9d49`**

**产品方向（变量 / 常量）**：用户已确认梵澄核心竞争力在**变量层**——政策、新闻、情绪、量价等每日综合出的 outlook → 哲学 filter → quant gate；**常量层**为缠论 S/R 入场、SL、5m 退出、沉淀资金过滤等执行纪律（可执行、非 alpha）。baseline 模式 `fundamental_chan` 即该分工的实现；后续优先强化研判与 outbox/实盘桥，而非堆叠固定策略模板。详见 **[PRODUCT_PHILOSOPHY_VARIABLE_CONSTANT.md](./PRODUCT_PHILOSOPHY_VARIABLE_CONSTANT.md)**。

---

## 1. 生产状态（当前 baseline）

| 项 | 值 |
|----|-----|
| **Quant 模式** | `QUANT_MODE=fundamental_chan` |
| **品种池** | 原始 72 → 沉淀资金 **≥40亿** 保留 **57** → 永久黑名单 **`ec`** → **56 品种** 实际交易 |
| **回测窗口** | 2023-01-01 → 2025-12-31 |
| **官方 baseline** | **+102.26%** · **9,726** 笔 · **47.15%** 胜率 · **2.40%** 最大回撤 |
| **资金** | 100 万 → 2,022,609.85 元 |
| **退出路径** | `CHAN_EXIT_PATH=5m`（5m tick bar → hour 派生 → 日 K 代理；日志 `fallback=5m15m-cache`） |
| **贵金属 PnL** | **AU +8,940**（65 笔，66.15% 胜）· **AG +19,021**（71 笔，63.38% 胜） |

**退出结构（当前 baseline）：** 止损 6,479 · 止盈 2,632 · 移动止损·支撑跌破 351（**≈3.6%**）· 收盘平仓 264 · 平均持仓 **~85 分钟**

---

## 2. 指标时间线（同窗口 2023–2025，除非注明）

| 阶段 | 范围 | 收益 % | 笔数 | 胜率 % | 最大 DD % | 备注 |
|------|------|--------|------|--------|-----------|------|
| Daily-proxy 冒烟 | 3 品种 au/ag/rb | **+168.26** | — | — | — | EM-only 分钟 K，偏乐观 |
| 回归（intraday bug） | 里程碑 | **−2.34** | — | — | — | 真实 tick 分钟 K 首次全池 |
| Pre-backfill 代理 | 72 品种 | +2078.18 | — | — | — | 日 K 代理为主 |
| 全 zip flush 后 | 72 品种 | +399.00 | — | — | — | 混合 tick + 代理 |
| ZCE-fix 真实 tick | 72 品种 | **+292.26** | ~12,757 | ~45.9 | — | 真实 tick 路径 |
| 2026-06-21 恢复基线 | 72 品种 | **+56.74** | 12,907 | 45.91 | — | tick 现实基线 |
| 沉淀资金 ≥40亿 | 57 品种 | +25.90 | 10,566 | 47.86 | 12.57 | 含 ec |
| 56 品种 + 5m exit | ec 剔除 | **+97.96** | 10,212 | 47.09 | 1.50 | 5m 退出路径启用 |
| P0 修复 `243a9d49` | 56 品种 | **+97.37** | 10,028 | 47.00 | 2.47 | 夜盘 20:55 + S/R 方向 |
| **贵金属 + trail（CURRENT）** | 56 品种 | **+102.26** | **9,726** | **47.15** | **2.40** | `QUANT_PRECIOUS_MIN_RR=2.0` + `CHAN_TRAIL_ARM_RATIO=0.67` |

**关键跃迁：**
- ec 黑名单 + 5m exit：+25.9% → **+97.96%**（+72 pp）
- P0 修复：97.96% → 97.37%（−0.59 pp，−184 笔；退出重分类为主）
- 贵金属 RR + trail：97.37% → **+102.26%**（+4.89 pp，−302 笔）

---

## 3. 关键配置

### 环境变量

| 变量 | Baseline 值 | 说明 |
|------|-------------|------|
| `FANCHENG_DATA_DRIVE` | `E` | 数据在 E 盘 |
| `QUANT_MODE` | `fundamental_chan` | 探针 / 仿真默认 |
| `PHILOSOPHY_FILTER_V2` | `1` | 探针脚本自动设置 |
| `MODEL_C_LIVE_TIER` | `high_hit` | 探针默认 |
| `MODEL_C_SIM_TIER` | `sim_relaxed` | 探针默认 |
| `QUANT_PRECIOUS_MIN_RR` | **2.0** | au/ag 最低 reward:risk；`0` 关闭 |
| `CHAN_TRAIL_ARM_RATIO` | **0.67** | trail 盈利保护距离缩放 |
| `CHAN_EXIT_PATH` | **5m** | 默认 5m tick 退出路径 |
| `QUANT_EXCLUDE_IDS` | （可选） | 临时排除；`ec` 在代码永久黑名单 |

### 代码常量（`services/quant-trading-margin.js` / `chan-multitf-levels.js`）

| 项 | 值 |
|----|-----|
| `MIN_DEPOSIT_YUAN` | **4e9**（40 亿元） |
| `QUANT_INSTRUMENT_BLACKLIST` | **`['ec']`** |
| **ag 止损** | **300 元/手**（20 元/千克） |
| **au 止损** | **500 元/手**（0.5 元/克） |
| **rb 止损** | **3 价格点**（30 元/手） |
| ag 盈利保护 | 400 元/手（SL×4/3） |
| au 盈利保护 | 500 元/手 |
| rb 盈利保护 | 4 价格点 |

配置详情见 `docs/QUANT_TRADING_FUNDAMENTAL_CHAN.md`。

---

## 4. 基础设施（数据 / 退出 / P0）

| 组件 | 状态 |
|------|------|
| **Tick zip 回填** | **721/721** zips processed，`failed=0`（`tick-intraday-progress.json`） |
| **品种 5m 缓存** | **72/72** 品种有 tick intraday 5m cache |
| **ZCE 补漏** | 第二遍 force run 完成（`_backfill-tick-zce-missing-force-run.txt`） |
| **EM 增量合并** | `sync-intraday-klines.js` / `daily-data-sync` 步骤 `intraday_klines`；EM ~1 年窗口内覆盖，窗口外保留 `tick-zips-sync` |
| **5m 退出路径** | `CHAN_EXIT_PATH=5m`；入场 S/R fallback：5m/15m 缓存 → hour 派生 → 日 bar OHLC |
| **P0 夜盘修复** | commit **`243a9d49`**：20:55 session-close bug、S/R 方向对齐；asar 已 patch |

**数据路径：**
- 日 K：`data/klines/commodity-{id}-day.json`
- 5m/15m/hour：`data/klines/commodity-{id}-{5m,15m,hour}.json`
- 成交存档：`E:\FanchengFinance\data\history\quant-trading-archive\trades.jsonl`

---

## 5. UI / 导出（本 session patch）

| 功能 | 说明 |
|------|------|
| **合约标签** | `services/quant-trading-contract-display.js` → UI 显示 **沪金2404** 等具体合约月 |
| **退出原因中文** | `stop_loss→止损` · `take_profit→止盈` · `trail_support_break→移动止损·支撑跌破` · `session_close→收盘平仓` |
| **导出目录** | 默认 **`E:\交易记录`**（`getTradingRecordsExportDir()` in `data-paths.js`） |
| **流式导出** | CSV/JSON 大文件 stream export（`quant-trading-archive.js`） |
| **UI 脚注** | 存档路径 + 导出路径已在 quant 面板 footnote 展示 |
| **Asar patch** | `_patch-quant-trading-asar.js` 已重跑，嵌入 summary **102.261% / 9726** |

---

## 6. 关键文件索引

| 路径 | 角色 |
|------|------|
| `_probe-fundamental-chan-quant-summary.json` | **权威探针 summary**（`generatedAt` 2026-06-25T08:54:16Z） |
| `_quant-trading-backtest-summary.json` | App/UI 镜像（已同步 102.261% / 9726） |
| `E:\FanchengFinance\data\history\quant-trading-archive\trades.jsonl` | **9,726** 笔成交存档 |
| `docs/CHECKPOINT_2026-06-25.md` | 一页 checkpoint |
| `docs/SESSION_2026-06-18_QUANT_TICK_BACKFILL.md` | 完整 session 日志（tick 回填 → baseline 演进） |
| `docs/QUANT_TRADING_FUNDAMENTAL_CHAN.md` | 架构 / SL / 数据 / 探针说明 |
| `docs/PRODUCT_PHILOSOPHY_VARIABLE_CONSTANT.md` | **产品哲学**：变量层 vs 常量层分工 |
| `docs/QUANT_TRADING.md` | 运维 / UI / 任务计划 |
| `_analyze-trades-readthrough.json` | **过期**（10,212 笔 / +97.96%）— 需重跑 |
| `_analyze-quant-return-diagnosis.json` | **过期**（10,566 笔 / +25.9%）— 需重跑 |
| `_probe-fundamental-chan-quant-out.txt` | 最近一次探针 stdout |
| `_patch-quant-trading-asar-run.txt` | asar patch 日志 |
| `E:\FanchengFinance\data\history\trading\tick-intraday-progress.json` | tick 721/721 进度 |

**Analyze 脚本（repo root）：**
- `node _analyze-trades-readthrough.js` → `_analyze-trades-readthrough.json`
- `node _analyze-quant-return-diagnosis.js` → `_analyze-quant-return-diagnosis.json`

---

## 7. 已知问题 / 待办

| 项 | 严重度 | 说明 |
|----|--------|------|
| **trail 占比仍 ~3.6%** | 低 | 351/9726 笔为 `trail_support_break`；`CHAN_TRAIL_ARM_RATIO=0.5` 探针未跑，可能增加 trail 退出占比 |
| **Analyze JSON 过期** | 中 | 仍基于 10,212 或 10,566 笔旧 archive；baseline 已是 9,726 笔 |
| **QUANT_TRADING_FUNDAMENTAL_CHAN.md 部分段落** | 低 | Archive 行仍写 10,028 笔 pre-precious；summary JSON 已更新 |
| **Max DD 路径差异** | 信息 | 5m exit 后 DD 从 12.57%→1.50%→2.40%；指标定义随退出路径变化，live gate 需审慎 |
| **ec 永久黑名单** | 已处理 | TP 定价 bug，2023–2025 拖累 −179k |
| **无 git commit** | 信息 | 本 session 状态保存在 docs + JSON artifacts |

---

## 8. 新会话常用命令

### 8.1 审计 tick 库存（建议第一步）

```powershell
cd E:\FanchengFinance\source\fancheng-finance
$env:FANCHENG_DATA_DRIVE = "E"
npm run audit-tick-inventory
```

### 8.2 全量探针（baseline 复现）

```powershell
cd E:\FanchengFinance\source\fancheng-finance
Copy-Item _probe-fundamental-chan-quant-summary.json _probe-fundamental-chan-quant-summary-pre-rerun.json -ErrorAction SilentlyContinue
$env:FANCHENG_DATA_DRIVE = "E"
$env:QUANT_MODE = "fundamental_chan"
$env:QUANT_PRECIOUS_MIN_RR = "2.0"
$env:CHAN_TRAIL_ARM_RATIO = "0.67"
$env:CHAN_EXIT_PATH = "5m"
node scripts/probe-fundamental-chan-quant.js --from 2023-01-01 --to 2025-12-31 --compare-legacy 2>&1 | Tee-Object -FilePath _probe-fundamental-chan-quant-out.txt
```

### 8.3 可选：trail 探针（`CHAN_TRAIL_ARM_RATIO=0.5`）

```powershell
$env:CHAN_TRAIL_ARM_RATIO = "0.5"
# 同上 probe 命令
```

### 8.4 Tick intraday 回填 / 续跑

```powershell
$env:FANCHENG_DATA_DRIVE = "E"
npm run backfill-tick-intraday
# P0 仅 au/ag/rb：
node scripts/backfill-tick-intraday.js --instruments au,ag,rb --from 2023-01-01 --to 2025-12-31
```

### 8.5 EM 增量同步

```powershell
$env:FANCHENG_DATA_DRIVE = "E"
node scripts/sync-intraday-klines.js
# 或纳入日更：npm run daily-data-sync
```

### 8.6 刷新 analyze JSON

```powershell
cd E:\FanchengFinance\source\fancheng-finance
$env:FANCHENG_DATA_DRIVE = "E"
node _analyze-trades-readthrough.js
node _analyze-quant-return-diagnosis.js
```

### 8.7 Patch 生产 asar + 重启

```powershell
cd E:\FanchengFinance\source\fancheng-finance
node _patch-quant-trading-asar.js
# 然后关闭旧进程，启动：
E:\FanchengFinance\启动梵澄金融-安全.bat
```

### 8.8 开发模式启动

```powershell
cd E:\FanchengFinance\source\fancheng-finance
$env:FANCHENG_DATA_DRIVE = "E"
npm start
```

---

## 9. 建议优先级（ ranked ）

1. **重跑 analyze JSON**（5 min）— 让 readthrough / diagnosis 对齐 9,726 笔 baseline，确认 trail ~3.6% 与佣金拖累
2. **`npm run audit-tick-inventory`** — 确认 721/721 + 72/72 仍完整，无新 zip 缺口
3. **可选 `CHAN_TRAIL_ARM_RATIO=0.5` 探针** — 评估 trail 占比与收益 trade-off（当前 0.67 已 +4.89 pp vs P0）
4. **生产 asar 验证** — 启动 `启动梵澄金融-安全.bat`，确认 quant 面板显示 +102.26% / 9726、合约标签、中文退出、导出到 `E:\交易记录`
5. **Live gate 设计** — max DD 指标随 5m exit 路径变化；定 live 阈值前需固定 exit path 与 DD 算法
6. **Phase 3+**（低优先）— live daemon 日内挂单、笔/段级缠论 S/R（见 `QUANT_TRADING_FUNDAMENTAL_CHAN.md` Phase 计划）
7. **T+1 权重** — 生产 v1.34.8 未动；T+1 75% 实验与 quant fundamental_chan 独立

---

## 10. 新聊天粘贴模板（5 行 starter）

```
工作区 E:\FanchengFinance\source\fancheng-finance，数据 E:\FanchengFinance\data，生产 v1.34.8。
Quant baseline：QUANT_MODE=fundamental_chan，56 品种（57 沉淀资金 − ec），2023–2025 探针 +102.26% / 9726 笔 / 47.15% 胜 / 2.40% DD。
关键 env：QUANT_PRECIOUS_MIN_RR=2.0，CHAN_TRAIL_ARM_RATIO=0.67，CHAN_EXIT_PATH=5m；au 500¥/手 SL，ag 300¥/手，rb 3 点。
请先读 docs/HANDOFF_2026-06-25.md 与 docs/CHECKPOINT_2026-06-25.md，再 npm run audit-tick-inventory，必要时重跑 analyze JSON。
目标：[在此填写你的具体任务]
```

---

*生成时间：2026-06-25 · 无 git commit*
